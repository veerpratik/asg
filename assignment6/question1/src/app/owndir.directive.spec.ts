import { OwndirDirective } from './owndir.directive';

describe('OwndirDirective', () => {
  it('should create an instance', () => {
    const directive = new OwndirDirective();
    expect(directive).toBeTruthy();
  });
});
